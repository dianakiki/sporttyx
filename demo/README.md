# SporttyX Demo

Демонстрационная версия UI приложения SporttyX для GitHub Pages.

## 🎯 Описание

Это статическая демо-версия интерфейса SporttyX, созданная для демонстрации возможностей UI без необходимости запуска backend-сервера. Все данные моковые и хранятся локально.

## ✨ Особенности

- ✅ Полностью изолирован от основного кода
- ✅ Использует моковые данные вместо реальных API
- ✅ Готов к деплою на GitHub Pages
- ✅ Поддержка Docker для локального запуска
- ✅ Современный UI с Tailwind CSS и Lucide Icons

## 📦 Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm start

# Приложение будет доступно по адресу http://localhost:3000
```

### Сборка для продакшена

```bash
# Создание production build
npm run build

# Результат будет в папке build/
```

## 🐳 Запуск с Docker

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Сборка и запуск
docker-compose up -d

# Приложение будет доступно по адресу http://localhost:3000

# Остановка
docker-compose down
```

### Вариант 2: Docker напрямую

```bash
# Сборка образа
docker build -t sporttyx-demo .

# Запуск контейнера
docker run -d -p 3000:80 --name sporttyx-demo sporttyx-demo

# Остановка
docker stop sporttyx-demo
docker rm sporttyx-demo
```

## 🚀 Деплой на GitHub Pages

### Автоматический деплой (рекомендуется)

1. **Включите GitHub Pages в настройках репозитория:**
   - Перейдите в Settings → Pages
   - В разделе "Source" выберите "GitHub Actions"

2. **Настройте секреты (если требуется):**
   - Перейдите в Settings → Secrets and variables → Actions
   - Добавьте необходимые секреты (если есть)

3. **Запустите деплой:**
   - Сделайте коммит в папку `demo/`
   - Push в ветку `main`
   - GitHub Actions автоматически соберет и задеплоит приложение

4. **Или запустите вручную:**
   - Перейдите в Actions
   - Выберите "Deploy Demo to GitHub Pages"
   - Нажмите "Run workflow"

### Ручной деплой

```bash
# Установите gh-pages
npm install -g gh-pages

# Из папки demo выполните
npm run deploy
```

## 📋 Доступные страницы

- `/` - Главная страница со списком мероприятий
- `/profile` - Профиль пользователя
- `/feed` - Лента активностей
- `/team/1` - Просмотр команды
- `/event/1` - Детали мероприятия

## 🎨 Технологии

- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Lucide React** - Иконки
- **React Router** - Маршрутизация

## 📁 Структура проекта

```
demo/
├── public/              # Статические файлы
├── src/
│   ├── api/            # Моковый API слой
│   ├── components/     # React компоненты
│   ├── layouts/        # Layouts
│   ├── mockData/       # Моковые данные
│   ├── App.tsx         # Главный компонент
│   └── index.tsx       # Точка входа
├── Dockerfile          # Docker конфигурация
├── docker-compose.yml  # Docker Compose конфигурация
├── nginx.conf          # Nginx конфигурация
└── package.json        # Зависимости
```

## 🔧 Конфигурация

### Изменение базового URL для GitHub Pages

В `package.json` измените поле `homepage`:

```json
{
  "homepage": "https://ваш-username.github.io/название-репозитория"
}
```

### Изменение базового пути в роутере

В `src/App.tsx` измените `basename`:

```tsx
<Router basename="/название-репозитория">
```

## 🎭 Моковые данные

Все моковые данные находятся в `src/mockData/mockData.ts`. Вы можете изменить их для демонстрации различных сценариев.

## 🐛 Отладка

### Проблемы с GitHub Pages

1. **404 при переходе по прямой ссылке:**
   - Убедитесь, что в `public/` есть файл `.htaccess` или используется правильная конфигурация nginx
   - GitHub Pages автоматически обрабатывает это через `index.html`

2. **Неправильные пути к ресурсам:**
   - Проверьте поле `homepage` в `package.json`
   - Проверьте `basename` в `Router`

3. **Не работает автодеплой:**
   - Проверьте, что GitHub Pages включен в настройках
   - Проверьте логи в разделе Actions
   - Убедитесь, что у workflow есть необходимые permissions

## 📝 Лицензия

Этот проект создан для демонстрационных целей.

## 🤝 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.
