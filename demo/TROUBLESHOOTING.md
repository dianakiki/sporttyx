# 🔧 Решение проблем при деплое

## ❌ Ошибка: "Get Pages site failed"

### Полный текст ошибки:
```
Error: Get Pages site failed. Please verify that the repository has Pages enabled 
and configured to build using GitHub Actions, or consider exploring the 
`enablement` parameter for this action.

Error: HttpError: Not Found
```

### Причина:
GitHub Pages не включен в настройках репозитория или выбран неправильный источник сборки.

### ✅ Решение (пошагово):

1. **Откройте настройки репозитория:**
   - Перейдите на https://github.com/ваш-username/sporttyx
   - Нажмите на вкладку **Settings**

2. **Перейдите в раздел Pages:**
   - В левом меню найдите и нажмите **Pages**

3. **Настройте источник:**
   - В разделе "Build and deployment"
   - В поле **Source** выберите **"GitHub Actions"**
   - ⚠️ НЕ выбирайте "Deploy from a branch"!

4. **Сохраните:**
   - Нажмите кнопку **Save**
   - Дождитесь обновления страницы

5. **Проверьте активацию:**
   - Вы должны увидеть сообщение о том, что Pages включен
   - URL вашего сайта будет отображен

6. **Перезапустите деплой:**
   - Перейдите в раздел **Actions**
   - Найдите failed workflow "Deploy Demo to GitHub Pages"
   - Нажмите на него
   - Нажмите **Re-run all jobs**

### Скриншот настроек:
```
Settings → Pages → Build and deployment
┌─────────────────────────────────────┐
│ Source: [GitHub Actions ▼]          │  ← Выберите это!
│                                      │
│ ☐ Deploy from a branch              │  ← НЕ это
└─────────────────────────────────────┘
```

## ❌ Другие частые ошибки

### 404 при открытии сайта

**Проблема:** Сайт задеплоился, но показывает 404.

**Решение:**
1. Проверьте `homepage` в `demo/package.json`:
   ```json
   "homepage": "https://ваш-username.github.io/sporttyx"
   ```
2. Проверьте `basename` в `demo/src/App.tsx`:
   ```tsx
   <Router basename="/sporttyx">
   ```
3. Убедитесь, что название репозитория совпадает

### Белый экран

**Проблема:** Сайт открывается, но показывает белый экран.

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Console
3. Проверьте вкладку Network на 404 ошибки
4. Обычно это проблема с неправильными путями - см. решение выше

### Actions не запускается

**Проблема:** После push ничего не происходит в Actions.

**Решение:**
1. Проверьте, что файл `.github/workflows/deploy-demo.yml` существует
2. Убедитесь, что изменения были в папке `demo/`
3. Проверьте, что у вас есть права на запуск Actions
4. Попробуйте запустить вручную: Actions → Deploy Demo → Run workflow

### Build fails

**Проблема:** Сборка падает с ошибками.

**Решение:**
1. Проверьте логи в Actions
2. Убедитесь, что локально проект собирается: `npm run build`
3. Проверьте, что все зависимости установлены
4. Проверьте синтаксис в компонентах

## 📞 Чеклист перед деплоем

Перед тем как пушить код, убедитесь:

- [ ] GitHub Pages включен в Settings → Pages
- [ ] Source установлен в "GitHub Actions"
- [ ] `homepage` в package.json правильный
- [ ] `basename` в App.tsx правильный
- [ ] Локально проект собирается без ошибок
- [ ] Файл workflow существует в `.github/workflows/`

## 🎯 Быстрая проверка

Выполните эти команды для проверки:

```bash
# Проверка сборки
cd demo
npm install
npm run build

# Если сборка успешна, можно деплоить
git add .
git commit -m "Deploy demo"
git push origin main
```

## 📚 Дополнительная помощь

- **Полная документация:** `../DEMO_DEPLOYMENT.md`
- **Быстрый старт:** `QUICKSTART.md`
- **Финальные шаги:** `FINAL_STEPS.md`

## 💡 Совет

Всегда сначала тестируйте локально с Docker:
```bash
cd demo
docker-compose up -d
```

Если работает локально на http://localhost:3000, то и на GitHub Pages будет работать!
