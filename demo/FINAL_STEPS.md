# 🎯 Финальные шаги для деплоя на GitHub Pages

## ✅ Что уже готово

- ✅ Демо-проект создан в папке `/demo`
- ✅ Все компоненты работают с моковыми данными
- ✅ Сборка успешно протестирована
- ✅ Docker конфигурация готова
- ✅ GitHub Actions workflow настроен
- ✅ Документация написана

## 🔧 Что нужно сделать перед деплоем

### 1. Обновите homepage в package.json

Откройте `demo/package.json` и измените строку 4:

```json
"homepage": "https://dianakiki.github.io/sporttyx"
```

Замените `dianakiki` на ваш GitHub username, если он другой.

### 2. Проверьте basename в App.tsx

Файл `demo/src/App.tsx` уже содержит:

```tsx
<Router basename="/sporttyx">
```

Если название репозитория другое, измените `/sporttyx` на `/название-вашего-репо`.

### 3. Включите GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Pages
3. В разделе "Build and deployment"
4. Source: выберите **"GitHub Actions"**
5. Сохраните

## 🚀 Деплой

### Вариант 1: Автоматический (рекомендуется)

```bash
# Из корня проекта sporttyx
git add .
git commit -m "Add demo version for GitHub Pages"
git push origin main
```

GitHub Actions автоматически:
1. Соберет проект
2. Создаст production build
3. Задеплоит на GitHub Pages

Проверьте статус в разделе **Actions** вашего репозитория.

### Вариант 2: Ручной деплой

```bash
cd demo
npm run deploy
```

Затем в Settings → Pages выберите:
- Source: Deploy from a branch
- Branch: gh-pages / (root)

## 📱 Проверка

После успешного деплоя (2-3 минуты) откройте:

```
https://dianakiki.github.io/sporttyx
```

Доступные страницы:
- `/` - Главная
- `/profile` - Профиль
- `/feed` - Лента активностей
- `/team/1` - Команда
- `/event/1` - Мероприятие

## 🐳 Локальное тестирование перед деплоем

### С Docker (рекомендуется):

```bash
cd demo
docker-compose up -d
```

Откройте http://localhost:3000

Остановка:
```bash
docker-compose down
```

### Без Docker:

```bash
cd demo
npm start
```

## 🔍 Проверка деплоя

1. **GitHub Actions:**
   - Перейдите в Actions
   - Найдите "Deploy Demo to GitHub Pages"
   - Убедитесь, что статус зеленый ✅

2. **GitHub Pages:**
   - Settings → Pages
   - Проверьте статус и URL

3. **Откройте приложение:**
   - Перейдите по URL
   - Проверьте все страницы
   - Убедитесь, что стили загружаются

## ⚠️ Если что-то не работает

### 404 ошибка
- Проверьте `homepage` в package.json
- Проверьте `basename` в App.tsx
- Убедитесь, что GitHub Pages включен

### Белый экран
- Откройте консоль браузера (F12)
- Проверьте ошибки загрузки
- Убедитесь, что пути правильные

### Actions не запускается
- Проверьте, что файл `.github/workflows/deploy-demo.yml` существует
- Убедитесь, что изменения в папке `demo/`
- Попробуйте запустить вручную: Actions → Run workflow

## 📚 Документация

- **Полное руководство:** `../DEMO_DEPLOYMENT.md`
- **Быстрый старт:** `QUICKSTART.md`
- **Информация:** `DEMO_INFO.md`
- **Сводка:** `SUMMARY.md`

## ✨ Готово!

После выполнения этих шагов ваше демо будет доступно онлайн и вы сможете делиться ссылкой для демонстрации UI!
