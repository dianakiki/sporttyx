# 📘 Руководство по деплою демо-версии SporttyX на GitHub Pages

## 🎯 Обзор

Демо-версия SporttyX - это статическое веб-приложение с моковыми данными, предназначенное для демонстрации UI без необходимости запуска backend-сервера.

## 📋 Предварительные требования

- Git установлен на вашем компьютере
- Аккаунт на GitHub
- Node.js версии 16 или выше (для локального тестирования)
- Docker (опционально, для локального запуска в контейнере)

## 🚀 Пошаговая инструкция по деплою

### Шаг 1: Подготовка репозитория

1. **Создайте репозиторий на GitHub** (если еще не создан):
   - Перейдите на https://github.com/new
   - Введите название репозитория (например, `sporttyx`)
   - Выберите публичный или приватный репозиторий
   - Нажмите "Create repository"

2. **Клонируйте или обновите локальный репозиторий:**
   ```bash
   # Если репозиторий новый
   git clone https://github.com/ваш-username/sporttyx.git
   cd sporttyx
   
   # Или если уже есть локальная копия
   cd /путь/к/sporttyx
   git pull origin main
   ```

### Шаг 2: Настройка GitHub Pages

1. **Перейдите в настройки репозитория:**
   - Откройте ваш репозиторий на GitHub
   - Нажмите на вкладку "Settings"

2. **Включите GitHub Pages:**
   - В левом меню выберите "Pages"
   - В разделе "Build and deployment"
   - В поле "Source" выберите **"GitHub Actions"**
   - Сохраните изменения

### Шаг 3: Настройка конфигурации

1. **Обновите `homepage` в package.json:**
   
   Откройте файл `demo/package.json` и измените поле `homepage`:
   
   ```json
   {
     "homepage": "https://ваш-username.github.io/sporttyx"
   }
   ```
   
   Замените `ваш-username` на ваш GitHub username.

2. **Проверьте basename в App.tsx:**
   
   Файл `demo/src/App.tsx` должен содержать:
   
   ```tsx
   <Router basename="/sporttyx">
   ```
   
   Замените `/sporttyx` на название вашего репозитория.

### Шаг 4: Деплой

#### Вариант A: Автоматический деплой через GitHub Actions (рекомендуется)

1. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Setup demo for GitHub Pages"
   git push origin main
   ```

2. **Проверьте статус деплоя:**
   - Перейдите в раздел "Actions" вашего репозитория
   - Найдите workflow "Deploy Demo to GitHub Pages"
   - Дождитесь завершения (обычно 2-3 минуты)
   - Зеленая галочка означает успешный деплой

3. **Откройте ваше приложение:**
   - Перейдите по адресу: `https://ваш-username.github.io/sporttyx`

#### Вариант B: Ручной деплой

1. **Установите зависимости:**
   ```bash
   cd demo
   npm install
   ```

2. **Выполните деплой:**
   ```bash
   npm run deploy
   ```

3. **Дождитесь завершения:**
   - Скрипт автоматически соберет проект и загрузит его в ветку `gh-pages`

4. **Настройте GitHub Pages (если еще не настроено):**
   - Settings → Pages
   - Source: Deploy from a branch
   - Branch: gh-pages / (root)
   - Save

### Шаг 5: Проверка

1. **Откройте приложение:**
   ```
   https://ваш-username.github.io/sporttyx
   ```

2. **Проверьте основные страницы:**
   - Главная страница: `/`
   - Профиль: `/profile`
   - Лента: `/feed`
   - Команда: `/team/1`
   - Мероприятие: `/event/1`

## 🐳 Локальное тестирование с Docker

Перед деплоем рекомендуется протестировать локально:

```bash
cd demo

# Запуск с Docker Compose
docker-compose up -d

# Откройте http://localhost:3000
```

Для остановки:
```bash
docker-compose down
```

## 🔄 Обновление демо

Для обновления демо-версии:

1. **Внесите изменения** в папке `demo/`

2. **Закоммитьте и запушьте:**
   ```bash
   git add demo/
   git commit -m "Update demo"
   git push origin main
   ```

3. **GitHub Actions автоматически задеплоит** обновления

## 🐛 Решение проблем

### Проблема: 404 при открытии страницы

**Решение:**
- Проверьте, что GitHub Pages включен
- Убедитесь, что в Settings → Pages выбран правильный source
- Проверьте поле `homepage` в `package.json`
- Проверьте `basename` в `App.tsx`

### Проблема: Белый экран

**Решение:**
- Откройте консоль браузера (F12)
- Проверьте ошибки загрузки ресурсов
- Убедитесь, что пути к файлам правильные
- Проверьте, что build прошел успешно в Actions

### Проблема: GitHub Actions не запускается

**Решение:**
- Проверьте, что файл `.github/workflows/deploy-demo.yml` существует
- Убедитесь, что у вас есть права на запуск Actions
- Проверьте, что изменения были в папке `demo/`
- Попробуйте запустить workflow вручную: Actions → Deploy Demo → Run workflow

### Проблема: Стили не применяются

**Решение:**
- Убедитесь, что Tailwind CSS правильно настроен
- Проверьте, что файлы `tailwind.config.js` и `postcss.config.js` существуют
- Пересоберите проект: `npm run build`

## 📊 Мониторинг

### Просмотр логов деплоя

1. Перейдите в раздел "Actions"
2. Выберите последний запуск workflow
3. Нажмите на job "build" или "deploy"
4. Просмотрите логи каждого шага

### Проверка статуса GitHub Pages

1. Settings → Pages
2. Вы увидите статус деплоя и URL приложения
3. Если есть ошибки, они будут отображены здесь

## 🎨 Кастомизация

### Изменение моковых данных

Отредактируйте файл `demo/src/mockData/mockData.ts`:

```typescript
export const mockUser = {
  firstName: "Ваше имя",
  lastName: "Ваша фамилия",
  // ... другие поля
};
```

### Добавление новых страниц

1. Создайте компонент в `demo/src/components/`
2. Добавьте роут в `demo/src/App.tsx`:
   ```tsx
   <Route path="/новая-страница" element={<НовыйКомпонент />} />
   ```

## 📝 Чеклист перед деплоем

- [ ] Обновлен `homepage` в `package.json`
- [ ] Проверен `basename` в `App.tsx`
- [ ] GitHub Pages включен в настройках
- [ ] Workflow файл существует в `.github/workflows/`
- [ ] Локально проект собирается без ошибок (`npm run build`)
- [ ] Протестировано локально с Docker (опционально)
- [ ] Все изменения закоммичены и запушены

## 🎉 Готово!

После успешного деплоя ваше демо будет доступно по адресу:
```
https://ваш-username.github.io/sporttyx
```

Поделитесь этой ссылкой для демонстрации UI вашего приложения!

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Просмотрите логи в GitHub Actions
3. Создайте Issue в репозитории с описанием проблемы
