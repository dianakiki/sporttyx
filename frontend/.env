# Backend API URL
# When running frontend locally, it will connect to backend in Docker on port 8082
REACT_APP_API_URL=http://localhost:8082

# Port for local development server (default is 3000)
PORT=3000
